export const urlType = {
    SEARCH_BY_USER: "/users",
    SEARCH_BY_REPOSITORY: "/repositories",
};

const searchType = {
    byUser: "SEARCH_BY_USER",
    byRepo: "SEARCH_BY_REPOSITORY",
};

export default searchType;
